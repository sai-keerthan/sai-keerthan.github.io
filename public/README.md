# Public assets

Drop your résumé here as `resume.pdf` — it will be served at `/resume.pdf` and is already wired to the "Résumé" button in the hero nav.

For a custom domain, also add a `CNAME` file containing your domain on a single line (e.g. `keerthankasula.dev`).
